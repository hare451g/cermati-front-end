import styled from 'styled-components';

const HighLightsHeadingText = styled.h1`
  font-weight: normal;

  margin: 0rem;

  color: ${(props) => props.theme.colors.text};
  text-align: center;
`;

export default HighLightsHeadingText;
